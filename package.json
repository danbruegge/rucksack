{
  "name": "rucksack",
  "version": "0.0.1",
  "description": "Convert your tabs into a compact list.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-ext -s ./src/ run --bc --url google.com --url danbruegge.com",
    "lint": "web-ext -s ./src/ lint"
  },
  "author": "Daniel Brüggemann",
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.25.0",
    "babel-preset-env": "^1.6.0",
    "web-ext": "^1.10.0"
  }
}

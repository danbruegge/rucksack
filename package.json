{
  "name": "rucksack",
  "version": "0.0.1",
  "description": "Convert your tabs into a compact list.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-ext run --bc -s ./src/"
  },
  "author": "Daniel Brüggemann",
  "license": "MIT",
  "dependencies": {
    "web-ext": "^1.10.0"
  }
}
